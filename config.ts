// NOTE - Something like an API key should not be stored in a file like this.
// It should be referenced in an environment variable which is filled at build time from GitHub Secrets.
// I am doing this for to same time.
export const apiKey = '7cdb8750271e42448ccd0ca8db5805ca'
export const apiBaseUrl = 'https://api.rawg.io/api'
